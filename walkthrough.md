# 🚀 Project Walkthrough: Advanced Diabetes Prediction System

এই প্রোজেক্টটিকে আপনার পছন্দ অনুযায়ী একটি এন্টারপ্রাইজ-লেভেল, ফুল-স্ট্যাক অ্যাপ্লিকেশনে রূপান্তর করা হয়েছে। নিচে বিস্তারিত বিবরণ দেওয়া হলো:

## 1. ⚡ Backend API (FastAPI)
- `backend/main.py`: Streamlit অ্যাপটিকে সরিয়ে একটি শক্তিশালী REST API তৈরি করা হয়েছে। 
- **CORS** এনাবল করা আছে যাতে যেকোনো ফ্রন্টএন্ড এর সাথে কানেক্ট করতে পারে। 
- `backend/schemas.py`: Pydantic মডেল দিয়ে ইনপুট ভ্যালিডেশন করা হয়েছে।
- `backend/predict.py`: এই ফাইলে আগের Ensemble মডেল এবং নতুন TabNet মডেল—উভয়কেই সাপোর্ট করার জন্য লজিক লেখা হয়েছে।

## 2. 🎨 Premium Glassmorphism UI (React + Vite)
- `frontend/src/App.jsx`: Next.js এর বদলে অনেক ফাস্ট এবং লাইটওয়েট **Vite + React** ব্যবহার করে একটি অত্যন্ত আকর্ষণীয় **Glassmorphism UI** তৈরি করা হয়েছে।
- ব্যবহারকারীরা এখন UI থেকেই `Ensemble` এবং `TabNet` মডেলের মধ্যে সুইচ করতে পারবে।
- SHAP ভ্যালু দিয়ে কোন ফিচারগুলো ডায়াবেটিসের ঝুঁকি বাড়াচ্ছে বা কমাচ্ছে, তা গ্রাফিক্যালভাবে দেখা যাবে।

## 3. 🧠 Deep Learning (TabNet)
- `train_tabnet.py`: গুগল এর তৈরি **TabNet** (ট্যাবুলার ডেটার জন্য বেস্ট নিউরাল নেটওয়ার্ক) মডেল ট্রেইন করার স্ক্রিপ্ট তৈরি করা হয়েছে।
- আপনি এই স্ক্রিপ্ট রান করলেই `artifacts/tabnet_model.zip` তৈরি হয়ে যাবে এবং API স্বয়ংক্রিয়ভাবে এটি ব্যবহার করবে।

## 4. ⚙️ MLOps & Data Drift
- `backend/drift_monitor.py`: **Evidently AI** ইন্টিগ্রেট করা হয়েছে।
- আপনার অরিজিনাল `diabetes.csv` ডেটার সাথে নতুন প্রেডিকশনের ডেটা তুলনা করে এটি একটি সুন্দর HTML ড্রিফট রিপোর্ট জেনারেট করবে। এতে আপনি বুঝতে পারবেন সময়ের সাথে মডেল আপডেট করা প্রয়োজন কি না।

## 5. 🌐 Cloud Deployment & CI/CD
- `Dockerfile`: ফুল-স্ট্যাক অ্যাপটি ডকারে চালানোর জন্য ডকারফাইল কনফিগার করা হয়েছে।
- `docker-compose.yml`: শুধুমাত্র `docker-compose up` কমান্ডেই ফ্রন্টএন্ড এবং ব্যাকএন্ড একসাথে রান করবে।
- `.github/workflows/deploy.yml`: গিটহাবে কোড পুশ করলে যেন অটোমেটিক টেস্টিং এবং বিল্ড হয়, তার জন্য **GitHub Actions** যুক্ত করা হয়েছে।

## 6. 📚 Documentation
- একটি প্রফেশনাল এবং আন্তর্জাতিক মানের `README.md` তৈরি করা হয়েছে, যা আপনার গিটহাব প্রোফাইলের জন্য পারফেক্ট।

---

### পরবর্তী করণীয়:
আপনার কনসোলে ব্যাকগ্রাউন্ডে পাইথনের কিছু ডিপেন্ডেন্সি ইনস্টল হচ্ছে (PyTorch, Evidently)। এগুলো ইনস্টল হওয়া শেষ হলে আপনি নিচের কমান্ড দিয়ে TabNet মডেল ট্রেইন করতে পারবেন:

```bash
python train_tabnet.py
```

এরপর আপনি `docker-compose up` দিয়ে পুরো প্রোজেক্ট একবারে চালাতে পারবেন।
